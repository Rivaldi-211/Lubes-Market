@extends('layouts.public')
@section('title',$produk->nama_produk.' — BUMDes Berkah')
@section('content')
<section class="product-detail-section"><div class="shell"><x-flash/><div class="breadcrumbs"><a href="{{ route('home') }}">Beranda</a><span>/</span><a href="{{ route('catalogue') }}">Katalog</a><span>/</span><span>{{ $produk->nama_produk }}</span></div><div class="product-detail-grid"><div class="product-detail-media">@if($produk->foto)<img src="{{ asset('storage/'.$produk->foto) }}" alt="{{ $produk->nama_produk }}">@else<div class="product-placeholder detail-placeholder"><span>{{ strtoupper(substr($produk->nama_produk,0,1)) }}</span><small>Produk Lokal BUMDes Berkah</small></div>@endif</div><div class="product-detail-info"><div class="eyebrow"><span></span>{{ $produk->kategori->nama_kategori }}</div><h1>{{ $produk->nama_produk }}</h1><p class="seller-line">Dibuat oleh <a href="{{ route('catalogue',['q'=>$produk->umkm->nama_umkm]) }}">{{ $produk->umkm->nama_umkm }}</a></p><div class="rating-line">@if($produk->ulasan_count)<span><i class="bi bi-star-fill"></i> {{ number_format((float)$produk->ulasan_avg_rating,1) }}</span><span>{{ $produk->ulasan_count }} ulasan</span>@else<span>Belum ada ulasan</span>@endif</div><div class="price-line">Rp{{ number_format((float)$produk->harga,0,',','.') }}</div><x-stock-badge :product="$produk"/><p class="product-description">{{ $produk->deskripsi }}</p>@if($produk->isAvailable())<form class="buy-box" action="{{ route('cart.add',$produk) }}" method="post">@csrf<label>Jumlah<input type="number" name="jumlah" min="1" max="{{ $produk->stok_jumlah }}" value="1"></label><button class="button button-dark" type="submit"><i class="bi bi-bag-plus"></i> Tambah ke Keranjang</button></form>@else<div class="unavailable-note">Produk sedang tidak tersedia. Cek kembali katalog untuk pilihan lain.</div>@endif<div class="seller-card"><div class="seller-avatar">{{ strtoupper(substr($produk->umkm->nama_umkm,0,1)) }}</div><div><small>Mitra UMKM</small><strong>{{ $produk->umkm->nama_umkm }}</strong><p>{{ $produk->umkm->alamat }}</p></div></div></div></div></div></section>
<section class="section reviews-section"><div class="shell narrow"><div class="section-heading"><div><div class="eyebrow"><span></span>Pengalaman pembeli</div><h2>Ulasan produk</h2></div></div><div class="review-list">@forelse($produk->ulasan as $review)<article><div class="review-head"><div><strong>{{ $review->pembeli->nama_lengkap }}</strong><span>{{ $review->created_at->translatedFormat('d M Y') }}</span></div><div class="stars">@for($i=1;$i<=5;$i)<i class="bi {{ $i <= $review->rating ? 'bi-star-fill' : 'bi-star' }}"></i>@endfor</div></div><p>{{ $review->komentar ?: 'Pembeli memberi penilaian tanpa komentar.' }}</p></article>@empty<div class="empty-inline">Belum ada ulasan untuk produk ini.</div>@endforelse</div></div></section>
@if($related->isNotEmpty())<section class="section section-muted"><div class="shell"><div class="section-heading"><div><div class="eyebrow"><span></span>Masih satu kategori</div><h2>Produk lain yang mungkin cocok.</h2></div></div><div class="product-grid compact-grid">@foreach($related as $product)<x-product-card :product="$product"/>@endforeach</div></div></section>@endif
@endsection
