@extends('layouts.public')
@section('title','Keranjang — BUMDes Berkah')
@section('content')
<section class="inner-hero compact-hero"><div class="shell"><div class="eyebrow"><span></span>Keranjang</div><div class="inner-hero-row"><h1>Pesanan yang<br><em>sedang disiapkan.</em></h1><p>Periksa jumlah produk sebelum melanjutkan ke checkout.</p></div></div></section>
<section class="section cart-section"><div class="shell"><x-flash/>@if($items->isEmpty())<div class="empty-state"><i class="bi bi-bag"></i><h3>Keranjang masih kosong.</h3><p>Mulai dari katalog dan pilih produk yang ingin Anda pesan.</p><a class="button" href="{{ route('catalogue') }}">Buka katalog</a></div>@else<div class="cart-layout"><form class="cart-list" method="post" action="{{ route('cart.update') }}">@csrf @method('PATCH')@foreach($items as $item)@php($product=$item['product'])<article class="cart-row"><div class="cart-thumb">@if($product->foto)<img src="{{ asset('storage/'.$product->foto) }}" alt="{{ $product->nama_produk }}">@else<div class="product-placeholder"><span>{{ strtoupper(substr($product->nama_produk,0,1)) }}</span></div>@endif</div><div class="cart-product"><small>{{ $product->umkm->nama_umkm }}</small><h3><a href="{{ route('products.show',$product) }}">{{ $product->nama_produk }}</a></h3><x-stock-badge :product="$product"/></div><div class="cart-price">Rp{{ number_format((float)$product->harga,0,',','.') }}</div><label class="cart-qty">Jumlah<input type="number" name="jumlah_cart[{{ $product->id }}]" min="0" max="{{ $product->stok_jumlah }}" value="{{ $item['quantity'] }}"></label><div class="cart-total">Rp{{ number_format((float)$item['line_total'],0,',','.') }}</div><button class="icon-danger" type="submit" form="remove-{{ $product->id }}" aria-label="Hapus {{ $product->nama_produk }}"><i class="bi bi-trash3"></i></button></article>@endforeach<div class="cart-actions"><button class="outline-link" type="submit"><i class="bi bi-arrow-repeat"></i> Perbarui jumlah</button></div></form>@foreach($items as $item)<form id="remove-{{ $item['product']->id }}" method="post" action="{{ route('cart.remove',$item['product']) }}">@csrf @method('DELETE')</form>@endforeach<aside class="cart-summary"><span>Ringkasan</span><h2>Keranjang Anda</h2><div class="summary-line"><span>{{ $items->sum('quantity') }} item</span><strong>Rp{{ number_format($subtotal,0,',','.') }}</strong></div><p>Harga belum termasuk biaya pengantaran bila ada. Metode pembayaran dipilih pada langkah berikutnya.</p>@auth @if(auth()->user()->isBuyer())<a class="button button-dark wide" href="{{ route('checkout.create') }}">Lanjut checkout <i class="bi bi-arrow-right"></i></a>@else<a class="button button-dark wide" href="{{ route('login') }}">Gunakan akun pembeli</a>@endif @else<a class="button button-dark wide" href="{{ route('login') }}?next=checkout">Masuk untuk checkout <i class="bi bi-arrow-right"></i></a>@endauth<form method="post" action="{{ route('cart.clear') }}">@csrf @method('DELETE')<button class="clear-cart" type="submit">Kosongkan keranjang</button></form></aside></div>@endif</div></section>
@endsection
